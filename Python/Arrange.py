lt = eval(input("Enter Some List: "))
#print(lt is list)
def sort(ls): #this sort method will work only for string and int
	l1 = []
	l2 = []
	if type(ls)==list:
		while e in ls is int:
			l1.append(e)
		l.sort()
		while e in ls is str:
			l2.append(e)
		l2.sort()
		ls.clear()
		ls.extend(l1.extend(l2))
	else:
		print("Please Provide Only List")
		
		
sort(lt)